const Button = ({ text, handler }) => {
const  styles = {
    marginRight: "10px",
  };
  return (
    <div>
      <button onClick={handler}>{text}</button>
    </div>
  );
};

export default Button;
